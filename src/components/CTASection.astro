---
import { ctaSection } from "../data/content";
---

<section id="cta-final" class="section-padding relative overflow-hidden">
    <!-- No dark bg -->

    <!-- Decorative Glow (Removed as it is now global) -->

    <div class="container-narrow relative z-10">
        <div class="glass-card p-8 md:p-12 border-brand-100">
            <div class="grid lg:grid-cols-2 gap-12 items-center">
                <!-- Left Content -->
                <div class="text-center lg:text-left">
                    <span
                        class="text-accent-green font-bold tracking-wider uppercase text-sm mb-2 block"
                    >
                        Recupera tu inversión hoy
                    </span>
                    <h2
                        class="text-3xl sm:text-4xl font-bold text-brand-900 mb-6"
                    >
                        {ctaSection.title}
                    </h2>
                    <p class="text-lg text-brand-600 mb-8 leading-relaxed">
                        {ctaSection.subtitle}
                    </p>

                    <ul class="space-y-4 mb-8 text-left inline-block">
                        <li class="flex items-center gap-3">
                            <span class="text-gold-500">✓</span>
                            <span class="text-brand-700"
                                >Sistema completo de branding</span
                            >
                        </li>
                        <li class="flex items-center gap-3">
                            <span class="text-gold-500">✓</span>
                            <span class="text-brand-700"
                                >Web y redes sociales listas</span
                            >
                        </li>
                        <li class="flex items-center gap-3">
                            <span class="text-gold-500">✓</span>
                            <span class="text-brand-700"
                                >Sin contratos forzosos</span
                            >
                        </li>
                    </ul>

                    <div
                        class="flex flex-col sm:flex-row gap-4 justify-center lg:justify-start"
                    >
                        <a
                            href={ctaSection.ctaHref}
                            class="btn-cta text-lg w-full sm:w-auto text-center"
                            style="background: linear-gradient(135deg, var(--color-gold-500), var(--color-gold-600)); color: black; font-weight: 800;"
                            data-track="cta_section_button_click"
                        >
                            {ctaSection.cta}
                        </a>
                    </div>
                    <p class="mt-4 text-sm text-brand-500 italic">
                        {ctaSection.note}
                    </p>
                </div>

                <!-- Right Content: Form/Card -->
                <div class="relative">
                    <div
                        class="bg-white p-6 rounded-xl border border-brand-100 shadow-xl"
                    >
                        <h3
                            class="text-xl font-bold text-brand-900 mb-4 text-center"
                        >
                            Inicia tu transformación
                        </h3>
                        <form
                            class="space-y-4"
                            onsubmit="event.preventDefault(); window.location.href='https://wa.me/19152955797?text=Quiero%20empezar%20mi%20transformaci%C3%B3n';"
                        >
                            <div>
                                <label
                                    class="block text-xs font-bold text-brand-500 uppercase mb-1"
                                    >Nombre del Restaurante</label
                                >
                                <input
                                    type="text"
                                    class="w-full bg-brand-50 border border-brand-200 rounded-lg px-4 py-3 text-brand-900 focus:border-gold-500 focus:outline-none transition-colors"
                                    placeholder="Ej. Tacos El Rey"
                                />
                            </div>
                            <div>
                                <label
                                    class="block text-xs font-bold text-brand-500 uppercase mb-1"
                                    >Tu Nombre</label
                                >
                                <input
                                    type="text"
                                    class="w-full bg-brand-50 border border-brand-200 rounded-lg px-4 py-3 text-brand-900 focus:border-gold-500 focus:outline-none transition-colors"
                                    placeholder="Tu nombre completo"
                                />
                            </div>
                            <div>
                                <label
                                    class="block text-xs font-bold text-brand-500 uppercase mb-1"
                                    >WhatsApp</label
                                >
                                <input
                                    type="tel"
                                    class="w-full bg-brand-50 border border-brand-200 rounded-lg px-4 py-3 text-brand-900 focus:border-gold-500 focus:outline-none transition-colors"
                                    placeholder="+1 (555) 000-0000"
                                />
                            </div>
                            <button
                                type="submit"
                                class="w-full bg-gold-500 hover:bg-gold-400 text-black font-bold py-4 rounded-lg shadow-lg shadow-gold-500/20 transition-all transform hover:scale-[1.02]"
                                data-track="cta_section_form_submit"
                            >
                                Solicitar Información →
                            </button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
