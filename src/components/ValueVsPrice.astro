---
import { valueVsPrice, paymentPlan } from "../data/content";
---

<section class="section-padding relative overflow-hidden" id="precio">
    <!-- No dark bg -->
    <!-- No dark bg -->
    <!-- Local blob removed as it is now global -->

    <div class="relative z-10 container-narrow">
        <h2
            class="text-3xl sm:text-4xl lg:text-5xl font-bold text-brand-900 text-center mb-4 animate-fade-in-up"
        >
            {valueVsPrice.title}
        </h2>
        <hr class="section-divider" />

        <div class="max-w-3xl mx-auto">
            <div
                class="glass-card p-10 text-center border-gold-400/30 animate-fade-in-up animate-delay-100"
            >
                <!-- Total value -->
                <p class="text-brand-600 text-lg mb-2">
                    Valor total de todos los servicios
                </p>
                <p
                    class="text-5xl sm:text-6xl font-black text-brand-200 line-through mb-6"
                >
                    {valueVsPrice.totalValue}
                </p>

                <!-- Final price -->
                <p class="text-brand-600 text-lg mb-2">Tu inversión hoy</p>
                <p
                    class="text-5xl sm:text-7xl font-black gradient-text-warm mb-4"
                >
                    {valueVsPrice.finalPrice}
                </p>

                <!-- Savings badge -->
                <div
                    class="inline-block px-5 py-2 mb-8 rounded-full bg-green-500/10 border border-green-500/30"
                >
                    <span class="text-green-600 font-bold">
                        Ahorras {valueVsPrice.savings} ({
                            valueVsPrice.savingsPercent
                        })
                    </span>
                </div>

                <!-- Payment Plan - PROMINENT -->
                <div
                    class="bg-brand-50 rounded-2xl p-8 mb-8 border border-gold-400/20 shadow-lg shadow-gold-500/5"
                >
                    <p
                        class="text-sm uppercase tracking-wider text-gold-600 font-semibold mb-3"
                    >
                        {paymentPlan.title}
                    </p>
                    <p
                        class="text-4xl sm:text-5xl font-black text-brand-900 mb-2"
                    >
                        {paymentPlan.firstPayment}
                    </p>
                    <p class="text-brand-600 text-lg mb-4">
                        {paymentPlan.note}
                    </p>
                    <div
                        class="flex items-center justify-center gap-3 flex-wrap"
                    >
                        {
                            paymentPlan.methods.map((method) => (
                                <span class="px-3 py-1 text-sm bg-white border border-brand-200 rounded-full text-brand-600">
                                    {method}
                                </span>
                            ))
                        }
                    </div>
                </div>

                <!-- CTA -->
                <a
                    href={valueVsPrice.ctaHref}
                    class="btn-cta text-lg"
                    style="background: linear-gradient(135deg, var(--color-mango), var(--color-terracotta)); box-shadow: 0 4px 20px rgba(243, 156, 18, 0.4); animation: pulse-glow 2s ease-in-out infinite;"
                    data-track="value_vs_price_cta_click"
                >
                    {valueVsPrice.cta}
                    <span class="text-2xl">→</span>
                </a>
            </div>
        </div>
    </div>
</section>
